/**
 * HOOKS - INDEX
 * 
 * Este archivo exporta todos los hooks del sistema de diseño atómico.
 * Los hooks manejan la lógica reutilizable del sistema.
 */

export { useScrollSpy } from './useScrollSpy';
export { useScrollTo } from './useScrollTo'; 